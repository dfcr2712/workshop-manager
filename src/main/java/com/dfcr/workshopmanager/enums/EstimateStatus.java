package com.dfcr.workshopmanager.enums;

/**
 * Pending - waiting for client decision
 * Approved - estimate approved by the client
 * Rejected - estimate rejected by the client
 */
public enum EstimateStatus {
    PENDING,
    APPROVED,
    REJECTED
}
